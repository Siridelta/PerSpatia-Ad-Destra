# 已归档文档

这些文件描述的是**更早的相机 / 视口方案**，与当前仓库中的 `CameraControl`（局部 Zustand）+ `ReactFlow3D` / `Scene3D` 实现**不一致**。

保留原因：部分坐标直觉、操作说明仍有参考价值；若需核对历史决策可在此查阅。

| 文件 | 大致年代 / 主题 |
|------|----------------|
| `camera-control-architecture.md` | 伪 3D、物理参数、事件流；文中仍引用已移除的 `useCameraControl` 路径 |
| `usePanAndZoomControl.md` | 旧钩子 `usePanAndZoomControl` 的用法 |
| `viewport-coordinate-system.md` | OrbitControls +「假缩放」时代的视口说明 |

现行说明请回到上一级目录阅读 **camera-architecture.md**。
